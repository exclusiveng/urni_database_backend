# Environment (development, production, test)
NODE_ENV=development
PORT=3000

# Database connection string
# For production, use your cloud database URL (e.g., from Render, Railway, etc.)
# Migrations will run automatically in production on server startup
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/urni_db
